[build]
  command = "npm run build:functions"
  functions = "functions-build"

# Route API requests and root path through the server function
[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
